<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ogan.codedenim">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Courses.CourseDetailActivity"
            android:parentActivityName=".Courses.CourseActivity" />
        <activity
            android:name=".Courses.CourseActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Login.CorperLoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".StarterActivity" />
        <activity
            android:name=".StarterActivity"
            android:theme="@style/AppTheme.NoActionBar.Starter">

        </activity>
        <activity
            android:name=".Login.StudentLoginActivity"
            android:label="@string/title_activity_student_login"
            android:parentActivityName=".StarterActivity" />
        <activity
            android:name=".Courses.CoursesByCategories"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Register.CorperReg"
            android:label="@string/title_activity_corper_reg"
            android:parentActivityName=".Login.CorperLoginActivity" />
        <activity android:name=".Courses.ModuleActivity"
            android:parentActivityName=".Courses.CourseDetailActivity"></activity>
    </application>

</manifest>